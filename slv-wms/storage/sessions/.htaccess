# Belt-and-braces deny — the root .htaccess already 404s /storage/* but
# session files are sensitive enough that we re-deny here.
Require all denied
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
